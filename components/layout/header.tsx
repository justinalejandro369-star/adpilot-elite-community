// Header removed — functionality moved to TopNav (top-nav.tsx)
// This file is kept to avoid import errors in case anything references it.
export function Header({ title }: { title?: string }) {
  void title;
  return null;
}
